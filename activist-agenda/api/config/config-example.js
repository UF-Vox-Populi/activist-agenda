export default {
    db: {
      //Place the uri for the mongo database here. The database need not be created beforehand, but it does need a name.
      // for example: 'mongodb://127.0.0.1:27017/UserDB'
      uri: ''
    }, 
    //Place the port for the Express server here. MAKE SURE IT IS DIFFERENT FROM THE REACT PORT. 5000 is best.
    port: 5000
};